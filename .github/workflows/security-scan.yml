name: Security Scan for Bluesky bot

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"

      - name: Generate SBOM with uv
        run: uv export --format cyclonedx --output-file sbom.json --no-dev --locked

      - name: Scan SBOM with Grype
        uses: anchore/scan-action@v7
        with:
          sbom: "sbom.json"
          fail-build: true
          severity-cutoff: "high"

      - name: Notify Discord on Failure
        if: failure()
        run: |
          curl -H "Content-Type: application/json" \
               -d '{"username": "Security Bot", "content": "⚠️ **脆弱性が検出されました**\nGitHub Actionsを確認してください: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"}' \
               ${{ secrets.DISCORD_WEBHOOK }}